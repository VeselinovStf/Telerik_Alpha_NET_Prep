//Write a JS function that takes three numbers as input and outputs their sum.

function sum(num1, num2, num3) {
	let sum = num1 + num2 + num3;
	return sum;
}

console.log(sum(2, 3, 4));
console.log(sum(1.5, 1.5, -1));